"""Panel widgets and input handling. Filled in Step 3.

Planned widgets:
  - Scenario panel (left): scenario preset buttons, boundary volume sliders,
    demand-surge and incident triggers.
  - Controls panel (right): click-an-intersection to open an editor with
    cycle slider, phase-split sliders, LT treatment dropdown, turn-restriction
    checkboxes, offset slider; plus the scoreboard with baseline/Webster/
    optimum comparison.
"""


def handle_event(event, state):
    """Route a pygame event to the appropriate widget. TODO(Step 3)."""
    pass
